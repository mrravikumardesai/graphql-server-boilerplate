import {mutations} from "./mutations"
import {queris} from "./queries"
import {resolvers} from "./resolvers"
import {typeDefs} from "./typedef"

export const user = {mutations, queris, resolvers, typeDefs}